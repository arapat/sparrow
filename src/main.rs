mod labeled_data; 
mod data_loader; 

// use labeled_data::LabeledData;

fn main() {
    // let a = vec![1.0, 2.0, 3.0];
    // let b = vec![1, 2, 3];
    // let c = LabeledData::new(a, b);
    println!("Just created a numbers object.");
}
